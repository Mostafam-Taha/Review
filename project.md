# مشاريع بايثون بعد إتقان الأساسيات

## 🟢 المشروع الأول (سهل) — To-Do List في الـ Terminal

**الفكرة:**
برنامج بسيط لإدارة مهامك اليومية من خلال الـ Command Line.

**المطلوب:**
- إضافة مهمة جديدة
- عرض كل المهام
- تحديد مهمة كـ "تمت"
- حذف مهمة
- حفظ المهام في ملف (txt أو json) عشان متتمسحش لما تقفل البرنامج

**المهارات اللي هتتدرب عليها:**
- Functions
- Lists / Dictionaries
- File handling (open, read, write)
- Loops و Conditions
- try/except للتعامل مع الأخطاء (زي إدخال رقم غلط)

**تحدي إضافي (اختياري):**
- إضافة تاريخ استحقاق لكل مهمة
- ترتيب المهام حسب الأولوية

---

## 🟡 المشروع الثاني (متوسط) — نظام إدارة مكتبة (Library System)

**الفكرة:**
برنامج يدير كتب مكتبة، مستخدمين، وعمليات استعارة/إرجاع.

**المطلوب:**
- إضافة/حذف/تعديل كتاب (اسم، مؤلف، عدد النسخ)
- تسجيل مستخدمين
- استعارة كتاب وإرجاعه (مع تحديث عدد النسخ المتاحة)
- البحث عن كتاب بالاسم أو المؤلف
- تخزين البيانات في ملف JSON أو قاعدة بيانات SQLite

**المهارات اللي هتتدرب عليها:**
- OOP (Classes) — كل كتاب/مستخدم Object
- SQLite أو JSON للتخزين
- Error handling أعمق
- تنظيم الكود في أكتر من ملف (modules)

**تحدي إضافي (اختياري):**
- إضافة نظام غرامات لو الكتاب اتأخر في الإرجاع
- عمل واجهة بسيطة بـ Tkinter بدل الـ terminal

---

## 🔴 المشروع الثالث (متقدم) — تطبيق ويب لتحليل بيانات (Data Dashboard)

**الفكرة:**
تطبيق ويب بسيط بياخد ملف CSV (مثلاً بيانات مبيعات) ويعرض تحليلات ورسوم بيانية تفاعلية.

**المطلوب:**
- رفع ملف CSV من المستخدم
- تنظيف البيانات (التعامل مع القيم الناقصة)
- عرض إحصائيات (متوسط، مجموع، أعلى/أقل قيمة)
- رسم بياني تفاعلي (خط، أعمدة، دائري)
- واجهة ويب بسيطة يقدر أي حد يستخدمها من المتصفح

**المهارات اللي هتتدرب عليها:**
- pandas لتحليل البيانات
- matplotlib أو plotly للرسم
- Streamlit أو Flask لعمل الواجهة
- التعامل مع رفع الملفات (file upload)
- هيكلة مشروع حقيقي (requirements.txt, app structure)

**تحدي إضافي (اختياري):**
- ربط البرنامج بـ API خارجي (زي بيانات الطقس أو أسعار العملات) بدل ملف CSV ثابت
- نشر (deploy) المشروع أونلاين عشان أي حد يقدر يفتحه بلينك

---

## نصيحة عامة
اشتغل بالترتيب: سهل → متوسط → متقدم. متستعجلش على المتقدم قبل ما تحس إنك مرتاح في الـ OOP والـ file handling. كل مشروع اعمله على GitHub عشان يبقى portfolio ليك.
